# PSI: train против valid (не month-over-month).
#
# Нужен datasets["valid"] в fit_select либо schema.split со строками valid.
# schema.time не обязателен. Ключ datasets["test"] не читается.
# На строковых категориальных approxQuantile падает — либо только numeric
# кандидаты, либо не ставьте этот шаг перед catboost_rfe с кошками.
# Для регрессии сэмпл не стратифицируется по таргету.

order:
  - psi: ${statistics.psi}

statistics:
  psi:
    mode: train_valid
    threshold: 0.25
    num_bins: 10
    relative_error: 0.001
    batch_size: 100
    n_jobs: -1
    subsample_rows: null

execution:
  seed: 42
  task_type: binary_classification
  max_local_rows: 1000000
